# NestJS API Production Environment Template — VIDYANTRA 2026 Code Relay
# Configure these environment variables in your cloud deployment platform (Render / Railway / AWS).
# NEVER COMMIT REAL PRODUCTION SECRETS TO SOURCE CONTROL.

DATABASE_URL=
JWT_SECRET=
PORT=3001
CORS_ORIGIN=
NODE_ENV=production
