{
  "name": "coderelay-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "VIDYANTRA 2026 Code Relay Competition Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "build": "npm run build:shared && npm run build:web && npm run build:api",
    "test": "npx ts-node apps/api/test/security-auth.test.ts && npx ts-node apps/api/test/organizer-console.spec.ts && npx ts-node apps/api/test/round-control-lifecycle.spec.ts && npx ts-node apps/api/test/round1-engine.spec.ts && npx ts-node apps/api/test/round2-engine.spec.ts && npx ts-node apps/api/test/round2-engine-300.spec.ts",
    "typecheck": "tsc --noEmit -p packages/shared && tsc --noEmit -p apps/web && tsc --noEmit -p apps/api",
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "prisma:migrate:deploy": "prisma migrate deploy --schema=prisma/schema.prisma",
    "prisma:db-push": "prisma db push --schema=prisma/schema.prisma",
    "prisma:seed": "prisma db seed --schema=prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=prisma/schema.prisma"
  },
  "prisma": {
    "seed": "npx ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.4.0",
    "typescript": "^5.7.3"
  }
}
